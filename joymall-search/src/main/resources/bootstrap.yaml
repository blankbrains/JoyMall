spring:
  cloud:
    nacos:
      discovery:
        server-addr: your-nacos-host:8848
      config:
        server-addr: your-nacos-host:8848
  application:
    name: joymall-search

server:
  port: 12001