server:
  port: 40000


spring:
  redis:
    host: your-redis-host
  session:
    store-type: redis
  cloud:
    sentinel:
      transport:
        port: 8719
        dashboard: your-sentinel-dashboard:8858


logging:
  level:
