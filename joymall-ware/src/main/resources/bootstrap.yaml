spring:
  application:
    name: joymall-ware
  cloud:
    nacos:
      discovery:
        server-addr: your-nacos-host:8848
      config:
        server-addr: your-nacos-host:8848
        file-extension: yaml
        group: dev
        namespace: your-namespace

#
#    alibaba:
#      seata:
#        tx-service-group: joymall-ware-fescar-service-group