spring:
  cloud:
    nacos:
      discovery:
        server-addr: your-nacos-host:8848
      config:
        server-addr: your-nacos-host:8848
        file-extension: yaml
        namespace: your-namespace
        group: dev
        extension-configs:
          - data-id: oss.yaml
            group: dev
            refresh: true

  application:
    name: joymall-third-party


